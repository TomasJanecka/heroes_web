export enum EHero {
    crystal, toxic, druid, knight
}

export enum ESidePanelItem {
    all= 'All',
    redCards = 'Red',
    blueCards = 'Blue',
    blackCards = 'Black',
    lightCards = 'Light',
    attackCards = 'attack',
    unitCards = 'Unit',
    disableCards = 'Disable',
    buffDebuffCards = 'Buff/Debuff'
}

export enum ECastType {
    red = 'red',
    blue = 'blue',
    black = 'black',
    light = 'light',
    none = 'none'
}